# ============================================
# SIFUT Backend Configuration
# ============================================
# このファイルをコピーして .env を作成してください
# cp .env.example .env

# ============================================
# Application
# ============================================
APP_NAME=SIFUT API
APP_VERSION=1.0.0
DEBUG=False
API_V1_PREFIX=/api/v1

# ============================================
# Database (PostgreSQL)
# ============================================
DATABASE_URL=postgresql://sifut_user:sifut_password_dev@localhost:5432/sifut

# ============================================
# Redis
# ============================================
REDIS_URL=redis://localhost:6379/0

# ============================================
# Security
# ============================================
# Generate a secure secret key:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-please-use-secure-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# Google OAuth (Required)
# ============================================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# ============================================
# AI Provider Configuration
# ============================================
# Choose one: claude, openai, gemini
AI_PROVIDER=claude

# Anthropic Claude
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-claude-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# OpenAI (Optional)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4o

# Google Gemini (Optional)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-pro

# ============================================
# Notion (Optional - for future integration)
# ============================================
NOTION_API_KEY=ntn_your-notion-api-key-here

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ============================================
# Usage Instructions
# ============================================
# 1. Copy this file to .env
#    cp .env.example .env
#
# 2. Update the following required values:
#    - SECRET_KEY (generate a secure random string)
#    - GOOGLE_CLIENT_ID
#    - GOOGLE_CLIENT_SECRET
#    - AI_PROVIDER
#    - ANTHROPIC_API_KEY (or OPENAI_API_KEY or GEMINI_API_KEY)
#
# 3. Start Docker services:
#    docker-compose up -d
#
# 4. Run database migrations:
#    cd backend
#    python -m venv venv
#    source venv/bin/activate  # On Windows: venv\Scripts\activate
#    pip install -r requirements.txt
#    alembic upgrade head
#
# 5. Start the backend server:
#    uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
#
# 6. Access API docs:
#    http://localhost:8000/docs (Swagger UI)
#    http://localhost:8000/redoc (ReDoc)
