# AIMeet Architecture

システム全体のアーキテクチャと設計思想

## 全体構成図

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                           │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Claude Desktop  │  │   CLI Tools     │  │  Webhook API    │ │
│  │   (MCP Client)  │  │ process-meeting │  │  (Cloud Run)    │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
└───────────┼────────────────────┼────────────────────┼──────────┘
            │                    │                    │
            │ MCP Protocol       │ Direct Call        │ HTTP/Pub/Sub
            ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Backend Services                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   MCP Server Layer                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │   Calendar   │  │     Meet     │  │   Events     │   │  │
│  │  │   MCP Tools  │  │  MCP Tools   │  │  MCP Tools   │   │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │  │
│  └─────────┼──────────────────┼──────────────────┼───────────┘  │
│            │                  │                  │              │
│  ┌─────────▼──────────────────▼──────────────────▼───────────┐  │
│  │              Service Orchestration Layer                   │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌─────────────┐ │  │
│  │  │ Meeting        │  │ Minutes        │  │ Integration │ │  │
│  │  │ Orchestrator   │  │ Generator      │  │ Services    │ │  │
│  │  └────────┬───────┘  └───────┬────────┘  └──────┬──────┘ │  │
│  └───────────┼────────────────────┼──────────────────┼────────┘  │
│              │                    │                  │           │
│  ┌───────────▼────────────────────▼──────────────────▼────────┐  │
│  │                 Core Service Layer                         │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────────┐  │  │
│  │  │ Google  │  │ Google  │  │ Google  │  │  Workspace  │  │  │
│  │  │  Auth   │  │Calendar │  │  Meet   │  │   Events    │  │  │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └──────┬──────┘  │  │
│  └───────┼────────────┼────────────┼───────────────┼─────────┘  │
└──────────┼────────────┼────────────┼───────────────┼────────────┘
           │            │            │               │
           │ OAuth 2.0  │ API        │ API           │ Pub/Sub
           ▼            ▼            ▼               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     External Services                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │  Google  │  │  Google  │  │  Google  │  │  Cloud       │   │
│  │  OAuth   │  │ Calendar │  │   Meet   │  │  Pub/Sub     │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘   │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                     │
│  │  OpenAI  │  │  Notion  │  │  Slack   │                     │
│  │   GPT    │  │   API    │  │ Webhook  │                     │
│  └──────────┘  └──────────┘  └──────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

## データフロー

### Phase 1: Calendar & Meet Creation

```
User
  │
  │ 自然言語指示
  │ "来週火曜15時にミーティングを作って"
  ▼
Claude Desktop (MCP Client)
  │
  │ Tool Call: create_event
  │ { summary, startTime, endTime, addMeetLink: true }
  ▼
MCP Calendar Server
  │
  │ OAuth 2.0 認証
  ▼
Google Calendar API
  │
  │ conferenceData.createRequest
  ▼
Google Meet
  │
  │ Meet Link 生成
  │ https://meet.google.com/xxx-xxxx-xxx
  ▼
User
  │
  │ イベント作成完了
  │ カレンダー招待送信
```

### Phase 2: Auto Meeting Minutes

```
会議開始
  │
  │ 文字起こし有効
  ▼
Google Meet (Recording & Transcript)
  │
  │ 会議終了
  │
  ▼
Conference Record 作成
  │
  │ Transcript 生成
  ▼
Workspace Events API
  │
  │ Event: transcript.fileGenerated
  ▼
Cloud Pub/Sub
  │
  │ Push notification
  ▼
Webhook Handler (Cloud Run)
  │
  │ Event payload
  ▼
Meeting Orchestrator
  │
  ├─► Meet Service
  │   │ Get transcript entries
  │   │ Format as text
  │   ▼
  │   Transcript Text
  │
  ├─► Calendar Service (optional)
  │   │ Get event details
  │   ▼
  │   Event metadata
  │
  ├─► Minutes Generator
  │   │ OpenAI API call
  │   │ GPT-4 + Custom template
  │   ▼
  │   Structured Minutes
  │   { summary, keyPoints, decisions, actionItems }
  │
  ├─► Notion Service
  │   │ Create page in database
  │   │ Format as blocks
  │   ▼
  │   Notion Page URL
  │
  └─► Slack Service
      │ Format as message
      │ Post via webhook
      ▼
      Slack Channel Notification
```

## モジュール構成

### src/services/

Google API とのインテグレーション層

- **google-auth.ts**: OAuth 2.0 認証管理
  - トークン取得・保存・更新
  - リフレッシュトークン処理
  - 認証状態チェック

- **calendar.ts**: Google Calendar API ラッパー
  - イベント CRUD 操作
  - Meet リンク付与
  - 定例ミーティング設定

- **meet.ts**: Google Meet API ラッパー
  - Conference Record 取得
  - Transcript 一覧・詳細取得
  - Participant 情報取得

- **workspace-events.ts**: Workspace Events API
  - Subscription 管理
  - Pub/Sub メッセージパース
  - イベントフィルタリング

- **notion.ts**: Notion API 連携
  - Database ページ作成
  - Block フォーマット
  - プロパティマッピング

- **slack.ts**: Slack Webhook 連携
  - Block Kit フォーマット
  - メッセージ投稿

### src/processors/

ビジネスロジック層

- **minutes-generator.ts**: OpenAI 議事録生成
  - テンプレートシステム
  - Prompt エンジニアリング
  - JSON 構造化出力

- **meeting-orchestrator.ts**: ワークフロー制御
  - サービス間の連携
  - エラーハンドリング
  - バッチ処理

### src/mcp/

MCP サーバー実装

- **calendar-server.ts**: MCP プロトコル実装
  - Tool 定義
  - Request handler
  - Stdio transport

### src/scripts/

CLI ツール

- **authenticate.ts**: 初回 OAuth フロー
- **process-meeting.ts**: 議事録生成 CLI

## 認証フロー

### OAuth 2.0 Desktop Flow

```
1. User runs: npm run auth
   └─► Generate authorization URL

2. Open browser
   └─► User authorizes app

3. Redirect to: http://localhost:3000/oauth2callback?code=xxx
   └─► User copies code

4. Enter code in CLI
   └─► Exchange code for tokens
       ├─► access_token (1 hour)
       └─► refresh_token (永続)

5. Save to token.json
   └─► Subsequent requests auto-refresh
```

### Token Management

- Access Token: 1時間で期限切れ → 自動リフレッシュ
- Refresh Token: 無期限（ユーザーが取り消さない限り）
- Storage: `token.json` (git-ignored)

## セキュリティ考慮事項

### 認証情報の保護

- `.env` と `token.json` は `.gitignore` に含まれる
- OAuth Client Secret は環境変数から読み込み
- MCP サーバーは stdio transport (ローカル実行)

### API キーの管理

- OpenAI API Key: 環境変数のみ
- Notion API Key: Integration 単位で制限
- Slack Webhook URL: チャンネル単位で制限

### データプライバシー

- Transcript は一時的にメモリ上でのみ処理
- OpenAI API: データ保持ポリシーに準拠（30日間）
- Notion: ユーザー管理下のワークスペース
- Slack: ユーザー管理下のワークスペース

## スケーラビリティ

### Phase 1-2 (現在)

- **実行環境**: ローカル MCP サーバー + CLI
- **処理能力**: 1会議ずつ手動処理
- **ユーザー**: 個人〜小規模NPO (5-10人)

### Phase 3 (計画)

- **実行環境**: Cloud Run + Pub/Sub
- **処理能力**: 並列処理・自動トリガー
- **ユーザー**: 中規模NPO (10-50人)
- **拡張機能**:
  - タスク管理連携
  - 日次サマリー
  - 多言語対応

### Future (将来)

- **実行環境**: Kubernetes + Message Queue
- **処理能力**: 大量会議の並列処理
- **ユーザー**: 大規模組織・エンタープライズ
- **拡張機能**:
  - リアルタイム文字起こし
  - 音声認識（Meet外でも）
  - カスタムAIモデル

## エラーハンドリング

### Graceful Degradation

配信先が失敗しても他は続行:

```typescript
try {
  await notionService.create(minutes);
} catch (error) {
  errors.push(`Notion failed: ${error}`);
  // Continue to Slack
}

try {
  await slackService.post(minutes);
} catch (error) {
  errors.push(`Slack failed: ${error}`);
}

return { minutes, errors }; // 部分的成功を返す
```

### Retry Strategy

- Google API: googleapis ライブラリの自動リトライ
- OpenAI API: 指数バックオフで最大3回
- Webhook: 即座に失敗（後でCLIから再実行可能）

## パフォーマンス

### レイテンシ

| ステップ | 時間 |
|---------|------|
| Transcript 取得 | ~2-5秒 |
| OpenAI 要約 (GPT-4) | ~10-30秒 |
| Notion 作成 | ~1-2秒 |
| Slack 投稿 | ~1秒 |
| **合計** | **~15-40秒** |

### コスト（目安）

**1会議あたり:**

- OpenAI (GPT-4, 10K tokens): ~$0.15
- Google Meet API: 無料枠内
- Notion API: 無料枠内
- Slack Webhook: 無料

**月間 50会議の場合:**
- ~$7.50/月

## 開発ロードマップ

### ✅ Phase 1 (Complete)
- [x] MCP Calendar & Meet server
- [x] イベント作成・管理
- [x] Meet リンク自動付与
- [x] Claude Desktop 統合

### ✅ Phase 2 (Complete)
- [x] Meet Transcript 取得
- [x] OpenAI 要約エンジン
- [x] Notion 連携
- [x] Slack 連携
- [x] CLI ツール

### 🚧 Phase 3 (In Progress)
- [ ] Workspace Events 自動化
- [ ] Cloud Run デプロイ
- [ ] タスク管理連携
- [ ] 日次サマリー

### 📋 Future
- [ ] リアルタイム文字起こし
- [ ] 多言語対応
- [ ] Google Docs エクスポート
- [ ] ダッシュボード UI
- [ ] プロジェクト横断分析

## 技術スタック

| レイヤー | 技術 |
|---------|------|
| 言語 | TypeScript 5.6+ |
| ランタイム | Node.js 18+ |
| API クライアント | googleapis, @notionhq/client, openai |
| MCP | @modelcontextprotocol/sdk |
| スキーマ検証 | Zod |
| デプロイ | Cloud Run (Phase 3) |
| メッセージング | Cloud Pub/Sub (Phase 3) |

## 参考資料

- [MCP Protocol Specification](https://spec.modelcontextprotocol.io/)
- [Google Meet API](https://developers.google.com/meet/api)
- [Google Calendar API](https://developers.google.com/calendar/api)
- [OpenAI API](https://platform.openai.com/docs)
- [Notion API](https://developers.notion.com/)
